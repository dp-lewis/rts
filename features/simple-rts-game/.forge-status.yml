schema_version: 3
feature: simple-rts-game
feature_description: "Make a simple RTS game — local single-player vs AI, client-side Phaser, deterministic fixed-timestep simulation."
created_at: "2026-08-21"
last_updated: "2026-08-22T07:20:00Z"
feature_mode: standard
flow_mode: gated
v2_native: true
backfilled: false
speckit_mode: "classic"

scope:
  summary: "Local single-player skirmish vs a simple AI opponent. Client-side only for v1."
  in_play: ["Phaser 4 client", "deterministic simulation core", "replay/command-log harness"]
  out_of_scope_v1: ["Node.js/Express backend", "MongoDB persistence", "online multiplayer / lockstep networking"]
  note: "Project config declares Express + MongoDB in project_tech_stack; the v1 scope chosen at intake excludes them. Config left unchanged."
  product_name: "Ten Minute War"
  product_name_note: "Renamed from '8 Bit RTS' 2026-08-21, resolving locked-spec open question 4. The old name promised pixel art the Kenney sprites do not deliver; the new one states the differentiator research identified (session length, not genre). Applied across 15 files including .product-forge/config.yml project_name. No requirement, story, journey, or non-goal changed - handled as a deferred-question resolution, not a change request."

phases:
  problem_discovery:
    status: completed
    started_at: "2026-08-21T09:48:53Z"
    completed_at: "2026-08-21T09:52:33Z"
    digest_path: "problem-discovery/problem-statement.md"
  research:
    status: completed
    started_at: "2026-08-21T09:56:32Z"
    completed_at: "2026-08-21T10:04:26Z"
    digest_path: "research/digest.md"
  product_spec:
    status: completed
    started_at: "2026-08-21T10:09:38Z"
    completed_at: "2026-08-21T10:23:01Z"
    digest_path: "product-spec/digest.md"
  design_system_harvest:
    status: not_applicable
    note: "Canvas game with no in-code component library. Visual system is the Kenney CC0 sprite pack (research/assets.md). No mockups/ or component-map.yml produced; journey steps use data-testid selectors in place of CMP- ids."
  revalidation:
    status: approved
    started_at: "2026-08-21T10:28:23Z"
    completed_at: "2026-08-21T10:35:38Z"
    digest_path: "review.md"
    revisions: 1
    locked: true
  bridge:
    status: completed
    started_at: "2026-08-21T10:36:49Z"
    completed_at: "2026-08-21T10:43:28Z"
    digest_path: "spec.md"
  i18n_harvest:           { status: not_applicable }
  plan:
    status: completed
    started_at: "2026-08-21T10:49:55Z"
    completed_at: "2026-08-21T10:59:39Z"
    digest_path: "plan/digest.md"
    adrs: ["plan/adr/ADR-001-canonical-state-hash.md", "plan/adr/ADR-002-replay-corpus.md"]
    complexity_tracking_entries: 0
  tasks:
    status: completed
    started_at: "2026-08-21T11:05:50Z"
    completed_at: "2026-08-21T11:12:04Z"
    digest_path: "tasks/digest.md"
    total: 82
    groups: 10
    story_coverage: "12/12"
    requirement_coverage: "33/33"
    test_first_tasks: 24
    xl_tasks: 0
  migration_plan:
    status: not_applicable
    note: "plan.md declares Storage: none. No database, no schema, no migrations (Express/MongoDB are explicit v1 non-goals). Conditional trigger not met."
  pre_impl_review:
    status: completed
    started_at: "2026-08-21T11:12:41Z"
    completed_at: "2026-08-21T11:22:29Z"
    scope: "focused — untested assumptions + consolidated risk register; architecture deliberately not re-treaded (covered by two Constitution Check gates)"
    digest_path: "pre-impl-review.md"
    findings: {high: 3, medium: 3, low: 1}
  implement:
    status: milestone_complete
    started_at: "2026-08-22T06:15:00Z"
    milestone_completed_at: "2026-08-22T09:05:00Z"
    digest_path: "implement/digest.md"
    run_scope: "M0 only (T001-T006) — enforcement skeleton. User-scoped at the Phase 6 kickoff gate."
    milestones_complete: ["M0", "M1", "M2", "M3"]
    red_gate:
      policy: per_milestone
      policy_rationale: "Greenfield repo: no package.json, no Vitest, no src/. The 27 Test-first tasks span M0-M9 and reference modules that will not exist for days, so a single upfront Red would be 27 bare import errors — a vacuous Red. Instead T001/T002 (pure INFRA, zero product code) scaffold the runner, then each milestone's test block must be observed failing before its implementation block runs. Chosen by the user at the Phase 6 kickoff gate 2026-08-22."
      status: confirmed_failing
      tests: ["T003 (tests/lint/boundary.test.ts)", "T007-T014 (rng, hash, commands, determinism, roundtrip, headless)", "T026-T030 (pathfind, economy, ordering)", "T034-T038 + T078-T080 (combat, production, victory)"]
      observed_at: "2026-08-22T06:17:37Z"
      evidence: "5 failed | 1 passed. Each failure read 'X was planted in src/sim/... and ESLint reported no error. Rules that did fire: (none)'. Green after T004: 6 passed (6). The one pass in the Red state is the scoping control, green by design in both states."
      m1_note: "Red rejected a SECOND time for the same reason: running the eight new test files against a repo with no src/sim modules produced bare 'Cannot find module' errors with zero new tests collected. Signature-only stubs (every export present, every body throwing) moved the failure to 43 collected tests failing on real calls. Writing the stubs also forced the whole M1 API surface to be settled before any logic existed. Green after T015-T025: 86 passed."
      note: "The first Red draft was REJECTED as insufficient — it failed with 'ESLint returned no result' (no config file, so ESLint matched nothing), which would have gone green on an empty eslint.config.js. Rewritten to lint against a parser-only rule-free fallback so a rule-less config still fails."
      skip_reason: null
    tasks_completed: 44   # corrective pass added no tasks; it repaired existing ones
    tasks_total: 82
    tasks_in_scope_this_run: 44
    progressive_checkpoints: 8
    progressive_warnings: 5
    progressive_critical: 0
    dependencies:
      added: 11
      vetted: 11
      warned: 0
      blocked: 0
      log_path: "features/simple-rts-game/dependency-log.md"
    sim_version: 6
    sim_version_history:
      - {version: 1, milestone: "M1", reason: "initial simulation behaviour"}
      - {version: 2, milestone: "M2 start", reason: "destX/destY added to Entity and the hash (ADR-001 Amendment 2). Deliberate regen, all 4 recorded hashes changed — the expected size for two new hashed fields per entity."}
      - {version: 6, milestone: "M3 corrective pass", reason: "Code-review CRITICAL/HIGH fixes: build and move commands now function, blocked-goal pathing no longer beelines through structures, deposit is cell-adjacency. Behaviour changed substantially — the corpus case's three build commands were previously silent no-ops, so its recorded hashes had been encoding broken behaviour since M1."}
      - {version: 4, milestone: "M3", reason: "ADR-001 Amendment 3 — queuedKind per entity, suddenDeathAt on state, and the two per-player indicator flags. The flags are hashed deliberately: FR-033 requires sudden-death damage NOT to read as under-attack, so they record what happened in the simulation rather than how it is drawn."}
      - {version: 5, milestone: "M3", reason: "ADR-001 Amendment 4 — gatherNodeId split out of targetId. targetId was holding two id spaces (entity ids and ore node ids) and combat cleared it out from under the economy the moment both existed."}
      - {version: 3, milestone: "M2", reason: "economy and movement wired into the tick. NOT bumped in the same edit at first: the corpus caught it as a REGRESSION (first divergence tick 100) rather than as staleness, which is the more useful failure. Bumped by hand, regenerated, hashes matched the regression's 'got' column exactly."}
    findings_for_gate:
      - id: "CR-M3-F5"
        severity: "resolved"
        title: "Removing the straight-line movement fallback exposed three further defects"
        detail: "Fixing REV-004 deleted a lenient fallback that had been masking two latent bugs, and a third surfaced only once move orders worked at all. (1) findPath required a PASSABLE START, but units stand inside their Base's blocked cell constantly — with the beeline gone every worker was trapped and the whole economy stopped. Blocked must mean cannot be ENTERED, not cannot be left. (2) Deposit range was measured in pixels to the Base's x/y while movement is cell-based, so whenever a Base was not exactly cell-centred the worker stood as close as it was allowed, was still too far, and oscillated IDLE<->MOVING forever holding a full load — a silent livelock with no error and no progress. Deposit is now cell adjacency. (3) The economy re-targeted a worker on the same tick a move order was issued (commands are stage 1, economy is stage 3), so orders looked ignored; the loop only ever sets a destination together with a gatherNodeId, so moving WITHOUT one is now the predicate for 'under human orders'."
        lesson: "Removing a lenient fallback is how you find out what it was hiding. All three were invisible while the beeline existed."
      - id: "M3-F1"
        severity: "resolved"
        title: "A test HUNG instead of failing"
        detail: "victory.test.ts used `while (verdict === NONE)` with no budget. Against an unimplemented victory stage it spun forever, synchronously, stalling the whole suite. Vitest's testTimeout CANNOT save you from this — a synchronous infinite loop blocks the event loop so the timeout never fires. In CI it would have burned the job timeout and reported nothing: no failing test name, no assertion, a dead job. The irony is exact: these are the CR-001 tests, whose entire purpose is that a match must terminate in bounded time."
        resolution: "Replaced with runToVerdict(state, budget), which throws a named error when the budget is exhausted, so non-termination is an assertion failure with a message rather than a hang."
      - id: "M3-F2"
        severity: "resolved"
        title: "The regression suite caught a real defect two milestones later"
        detail: "targetId held two different id spaces: M2 economy stored the worker's ore node in it, M3 combat stores an enemy entity id and clears it every tick for any unit with no enemy in range. Wiring combat into the tick made an M2 economy test fail. Entity ids start at 1 and the sentinel is -1, so in most configurations the collision would have HIDDEN — surfacing later as a rare seed-dependent worker stall rather than an obvious break."
        resolution: "Split gatherNodeId out of targetId (ADR-001 Amendment 4) rather than teaching combat to tiptoe around the economy's use of the field."
      - id: "M3-F3"
        severity: "resolved"
        title: "TC-UNIT-008 was an M3 exit criterion with no test, and step() did not enforce FR-004"
        detail: "plan.md lists TC-UNIT-008 (a command applies at its target tick, not on issue) among M3's exit criteria and nothing asserted it at the step() level. Worse, step() applied whatever commands it was handed regardless of tick, so FR-004 held only because every caller happened to filter correctly."
        resolution: "applyCommands now skips commands whose tick is not the current tick; a test offers the same command every tick and asserts it takes effect exactly once. The corpus did not go stale, confirming the guard changes nothing for correct callers."
      - id: "M3-F4"
        severity: "open — for M8 tuning or M9 playtest"
        title: "Workers auto-attack, which can starve the economy"
        detail: "ATTACK.worker exists and FR-020 says units auto-acquire enemies in range, so a worker beside an enemy flips to ATTACKING every tick and stops gathering for as long as the enemy stands there. Correct per the letter of FR-020, but a contested ore node could quietly halt a player's entire economy. Not speculating a fix."
      - id: "M2-F1"
        severity: "resolved"
        title: "The O-2 tie-break tests were passing for the wrong reason"
        detail: "Deleting the A* tie-break entirely left all 10 pathfinding tests green. The open set is a linearly scanned array, so among equal f the first-PUSHED candidate wins and push order is fixed by the neighbour loop — the search was deterministic for a reason unrelated to FR-022, and every path-level assertion passed whether the comparator was total or absent. Path output cannot detect this because the property is invisible unless the open set's order varies, which it never does today and would the moment anyone swapped in a binary heap. Same class of error as M0's empty-config Red."
        resolution: "Comparator exported as chooseBestOpen and tested directly against PERMUTED candidate order. Mutation-verified: tie-break deleted was 10 passed before, 3 failed after."
      - id: "M2-F2"
        severity: "resolved"
        title: "The baseline corpus case was testing a layout the game cannot produce"
        detail: "001-baseline was authored in M1 before a grid existed. Once M2 fixed the map at 20x11 tiles (1280x704 px), three of its six entities and one of its two ore nodes were off the map — player 1's economy and movement were entirely unexercised."
        resolution: "Re-authored as a proper mirror across x=640 on cell centres. Both sides now gather from tick 0 with no input (p0 +60 ore, p1 +30 over 400 ticks); both same-tick command collisions preserved with the AI listed first so O-4 stays a real guard."
      - id: "M2-F3"
        severity: "informational"
        title: "ADR-002 caught an undeclared behaviour change"
        detail: "Wiring economy into the tick changed behaviour without a SIM_VERSION bump in the same edit. The corpus failed as a regression rather than staleness — the correct and more useful failure, since it says 'behaviour moved and you did not declare it'. After the hand bump the message switched to the stale path and regeneration produced hashes identical to the regression's 'got' column. The discipline worked without anyone remembering to apply it."
      - id: "M1-F1"
        severity: "resolved"
        resolution: "ACCEPTED at the M1 gate 2026-08-22. ADR-001 Amendment 1 written into plan/adr/ADR-001-canonical-state-hash.md: fields 7 and 8 added to the hashed list, an Amendments section records what changed, why, how it was found, and how to revert, and the original field list is added to Alternatives rejected. Code comment in hash.ts updated to cite the amendment rather than propose it."
        title: "ADR-001 amendment — hash covers difficulty and nextEntityId"
        detail: "ADR-001 opens with 'exactly the simulation state' then lists fields omitting both. Neither is presentational or derived, so neither falls under the ADR's own 'what is NOT hashed' list — a drafting gap, not a deliberate exclusion. It matters: two states differing only in nextEntityId diverge at the next spawn, and difficulty is a field precisely so it can vary independently (FR-029). Implemented as a purely additive append AFTER the ADR's six fields; reverting is two lines plus one corpus:regen."
      - id: "M1-F2"
        severity: "deferred to M2 (decided)"
        resolution: "DECIDED at the M1 gate 2026-08-22: M2 owns movement and pathfinding, so M2 chooses the field shape, adds it to Entity and the hash, amends ADR-001, bumps simVersion, and regenerates the corpus — all in one reviewed change. Adding destX/destY now would mean guessing a shape before pathfinding has a design."
        title: "Data model has no destination field, so move orders cannot be recorded"
        detail: "plan.md's Entity and ADR-001's hashed field list both omit one. step() handles attack and build; move hits an explicit commented case that does nothing. Inventing destX/destY in M1 would add a hashed field ahead of the milestone that owns movement, staling every corpus hash recorded in between. M2 must decide the field and amend ADR-001 in the same change."
      - id: "M1-F3"
        severity: "informational"
        title: "The M0 boundary guard caught a real violation in M1 code"
        detail: "src/sim/replay.ts:126 used new Date().toISOString() for createdAt — a wall-clock read inside the simulation, caught by no-restricted-globals one milestone after the rule was written. Fixed by making createdAt caller-supplied; corpus.test.ts now fails any case whose date is not an ISO date, because a caller-supplied field is a forgettable one."
      - id: "M1-F4"
        severity: "informational"
        title: "navigator exists in Node 21+, so the lint ban on it is load-bearing"
        detail: "headless.test.ts asserted typeof navigator === 'undefined'; it is 'object', reporting Node.js/24. A sim module reading navigator would not crash under test — it would silently return one value in Node and another in the browser. The runtime environment cannot catch this; only the lint rule can."
    accepted_risks:
      - id: "O-7-partial"
        title: "Map/Set for-of guard is partial"
        decision: "accepted"
        decided_at: "2026-08-22T06:30:00Z"
        detail: "no-restricted-syntax bans ForInStatement outright and catches the .keys()/.values()/.entries() idiom plus iteration of a Map/Set literal. Bare `for (const x of someMap)` on a Map-typed variable is NOT caught — a syntax selector has no type information. Rejected escalations: type-aware linting via projectService (several times slower on every CI run, before any sim code exists to justify it) and banning Map/Set outright in src/sim (A* needs an open set; would force a workaround in pathfinding, where determinism bugs are most expensive)."
        mitigation: "The replay corpus (Constitution IV) fails the build when iteration order diverges across the CI matrix. Revisit only if O-7 actually bites; the escalation path is type-aware linting."
    ci:
      run_id: 32525241752
      url: "https://github.com/dp-lewis/rts/actions/runs/32525241752"
      commit: "99ec475"
      verdict: "success — 4/4 jobs green"
      jobs: ["ubuntu-latest node 22", "ubuntu-latest node 24", "macos-latest node 22", "macos-latest node 24"]
      boundary_guard_evidence: "Every runner logged: 'Lint correctly rejected a planted Math.random() in src/sim/__ci_canary__.ts.' The exit criterion is enforced on every push, not witnessed once."
      corpus_evidence: "FIRST cross-platform exact-bit hash agreement. The 001-baseline hashes were recorded locally on macOS/Node 24 and reproduced byte-identically on ubuntu/Node 22 and every other runner (13/13 corpus tests green on all four). This is the claim ADR-001 exists to make, and until this run it was untested."
      note: "actions/checkout and setup-node bumped v4 -> v5; --passWithNoTests removed from the corpus step now that a real case exists, so an accidentally-empty corpus directory fails instead of passing silently."
    open_tasks: []
  code_review:
    status: completed
    completed_at: "2026-08-22T09:40:00Z"
    scope: "M0-M3 (44/82 tasks). Reviewed at a milestone boundary by explicit request; implement.status is milestone_complete, not completed."
    artifact: "code-review.md"
  verify:                 { status: pending }
  test_plan:              { status: pending }
  test_run:               { status: pending }
  release_readiness:      { status: pending }
  monitoring_setup:       { status: pending }
  experiment_design:      { status: pending }
  spec_merge:             { status: pending }
  api_docs:               { status: not_applicable }
  security_check:         { status: pending }
  tracking_plan:          { status: pending }

problem:
  statement: "A casual player wanting an RTS fix in a spare 15 minutes has no maintained browser RTS that delivers a complete match arc."
  jtbd: "When I have 15 spare minutes and want something more strategic than a puzzle game, I want to jump straight into a complete RTS skirmish in a browser tab, so I can get the full build-up-and-win arc in one short sitting."
  segment: "Casual desktop browser players with RTS familiarity or nostalgia; short opportunistic sessions; no install, no account."
  severity: "4/10"
  validation: "Weak"
  go_decision: "go"
  go_basis: "Craft/capability goals (playable fun match + clean extensible codebase), not validated market pain. Severity deliberately not the gating factor."
  forces: { push: medium, pull: medium-high, inertia: high, anxiety: low-medium }
  risks:
    - "R1: Constitution mandates deterministic sim + replay regression (NON-NEGOTIABLE) but determinism was not a stated success outcome. Unbudgeted cost; must be resolved explicitly before Phase 5 Plan."
    - "R2: The real competitor is 'play nothing' at zero cost. Time-to-comprehension dominates; onboarding is make-or-break."
    - "R3: 'Simple' is undefined and RTS scope creeps. Needs a hard written v1 unit/resource/mechanic budget plus explicit non-goals."
    - "R4: 'Fun' is not derivable from a spec; needs a playtest-and-tune loop the plan must protect."
  hypotheses:
    H1: "Quick-session browser RTS niche is genuinely underserved (competitor analysis)."
    H2: "Radical mechanic reduction (~3 units, 1 resource, no fog of war) suffices for a satisfying match arc (UX research)."
    H3: "Deterministic fixed-timestep sim + seeded RNG + command log is achievable in Phaser 4 without fixed-point math (tech research)."
    H4: "Time-to-first-meaningful-action (<30s) and match-completion rate predict success better than strategic depth (metrics research)."

research_dimensions:
  competitors: completed
  ux_patterns: completed
  codebase: completed
  tech_stack: completed
  metrics_roi: completed
input_richness_score: 6

delta_specs:
  - "specs/simulation/spec.md"
  - "specs/gameplay/spec.md"
  - "specs/interface/spec.md"
contracts:
  openapi: not_applicable   # no HTTP surface; Express/MongoDB are v1 non-goals
  asyncapi: not_applicable  # no EDA patterns detected (research/codebase-analysis.md)
  note: "contracts/README.md records why, rather than emitting empty stubs that api-docs and sync-verify Layer 8 would validate against a fiction."

task_log: []
gates:
  - phase: problem_discovery
    decision: approved_with_conditions
    at: "2026-08-21T09:56:17Z"
    reviewed_sha: "2ac8aa052237fa0befb28de08a33e3ae39a2d84e"
    risk: low
    notes: "Go on craft grounds (playable fun match + clean extensible codebase), not on validated market pain. Severity 4/10, validation Weak — recorded honestly, deliberately not the gating factor."
    conditions:
      - "R1 RESOLVED — comply fully with constitution Principles I (Deterministic Simulation Core), II (Simulation-Presentation Separation) and IV (Replay-Verified Regression). No constitution amendment, no Complexity Tracking violation. Determinism is designed in from tick zero; the replay corpus grows from fixed defects. Rationale: I and II are near-free at greenfield and ruinous to retrofit, and II directly delivers the stated 'clean, extensible codebase' outcome."
      - "R2 CARRIED FORWARD — the real competitor is 'play nothing' at zero cost. Product spec must treat time-to-comprehension (<60s to legible play, <30s to first meaningful action) as a primary requirement, not a polish item."
      - "R3 CARRIED FORWARD — 'simple' must be pinned to a hard written v1 budget (unit count, resource count, mechanic list) with explicit non-goals, before Phase 5 Plan."
      - "R4 CARRIED FORWARD — plan must protect a playtest-and-tune loop; 'fun' is not verifiable by the traceability chain and will otherwise be dropped in favour of machine-checkable criteria."
  - phase: research
    decision: approved_with_conditions
    at: "2026-08-21T10:08:47Z"
    reviewed_sha: "2ac8aa052237fa0befb28de08a33e3ae39a2d84e"
    risk: low
    notes: "5/5 dimensions run (tech-stack + metrics opted in over config defaults to test H3/H4). All four hypotheses survived; H1 reframed (white space is session length, not genre), H4 split into sound-principle / unverifiable-measurement. 8 red flags raised."
    conditions:
      - "RF-1 RESOLVED — constitution amended to v1.1.0 (MINOR). Principle I now enumerates the supported-platform set: Node LTS on ubuntu-latest + macos-latest (headless) plus Chromium via Playwright; and states the deterministic/non-deterministic arithmetic boundary explicitly, forbidding transcendentals in sim code. Propagated to plan-template.md and tasks-template.md in the same change per the amendment procedure."
      - "RF-2 OPEN — A* open-set tie-breaking must use a stable rule (entity id) and carry a dedicated determinism test. Highest-probability determinism defect in the project."
      - "RF-3 OPEN — Phaser update(time, delta) idiom is unusable for sim per Principle I; mitigate by lint + explicit plan note, not vigilance."
      - "RF-4 OPEN — the 'fun' goal has a single manual check (comprehension playtest); it needs a protected slot in the plan or machine-checkable criteria will crowd it out."
      - "RF-5 OPEN — single-player-only forgoes the retention mechanism every competitor leads with; recorded as a knowing v1 trade-off, not an oversight."
      - "RF-6 OPEN — WebGL-unsupported is newly reachable (Phaser 4 deprecated Canvas); needs an honest fallback state."
      - "RF-7 OPEN — nothing instrumented (telemetry.* = none) and mobile benchmarks do not transfer; measure against ourselves via local counters + playtest."
      - "RF-8 OPEN — Kenney sprites appear to bake team colour in; ownership conveyed by colour alone violates WCAG 1.4.1. Non-colour differentiator required as an explicit spec requirement."
      - "ASSETS — Kenney 'RTS Pack: Sci-Fi' (CC0) added to repo at images/ mid-session: 48 unit / 16 structure / 42 tile / 20 environment sprites, uniform 64x64 (128x128 Retina). Settles art direction; supplies a natural 64px grid module for deterministic grid A*. Recorded in research/assets.md."
  - phase: product_spec
    decision: approved
    at: "2026-08-21T10:25:27Z"
    reviewed_sha: "2ac8aa052237fa0befb28de08a33e3ae39a2d84e"
    risk: low
    notes: "13 artifacts. R3 discharged — simple is now a hard written budget (1 resource, 4 unit types, 2 structures, 1 buildable, 1 fixed screen) with ~30 explicit non-goals. 26 FR-* minted; 3 journeys / 16 steps / 12 edges; traceability journeys block seeded. Sync-verify Layer 1 passed with one drift found and fixed (US-012 linked into JRN-001). 12/12 Must+Should stories journey-covered. design_system_harvest set not_applicable (canvas game, no component library) — no mockups or component-map produced, by decision."
    open_items:
      - "Balance numbers deliberately unspecified — feel is tuned, not specified (R4). Plan must reserve a tuning pass and the K1 comprehension playtest as scheduled work."
      - "Fixed single screen (~20x11 tiles at 64px) may feel cramped; playtest decides. Escalating to a scrolling map would be a change request."
      - "Difficulty gate is now the only pre-play friction, in tension with the no-tutorial decision. Accepted trade-off; anti-metric added."
      - "Open question: project name 'Ten Minute War' vs smooth-shaded sci-fi art direction."
  - phase: revalidation
    decision: approved
    at: "2026-08-21T10:35:38Z"
    reviewed_sha: "93ec5861bf45d7e317936582ef72f8dca58d67f1"
    risk: low
    revisions: 1
    notes: "Spec LOCKED after 1 revision. A determinism lens was applied to all 10 Must-Have acceptance criteria BEFORE the gate rather than at approval, asking of each: would two competent implementers build materially different things? Five would. All five resolved plus two minor ones. Final consistency pass clean: 16/16 cross-links, 3/3 wireframes, 12/12 Must+Should stories journey-covered, traceability edge counts matching."
    findings_resolved:
      - "D-1 HIGH — US-003 nearest ore node had no distance metric and no tie-break. Now least squared Euclidean distance, ties by ascending node id (FR-027). Third instance of the unspecified-ordering defect class in this feature, after FR-021 targeting and FR-022 A*."
      - "D-2 HIGH — spec said the build bar has exactly four entries; the wireframe drew five. Resolved IN FAVOUR OF THE WIREFRAME (4 unit + 1 visually separated structure), because the drawing was the better design."
      - "D-3 MED — US-001 and metrics K2 shared a 30s threshold while measuring different quantities, so one was guaranteed to be measured wrong. Split into <=10s time-to-interactive (guarantee) and <30s observed first action (playtest measure)."
      - "D-4 MED — simultaneous Base destruction was an unspecified branch. Now an explicit Draw verdict (FR-028, EDGE-009) rather than an invisible arbitrary tie-break."
      - "D-5 MED — difficulty is a field of initial simulation state alongside the RNG seed and appears in the replay header, not encoded into the seed integer (FR-029)."
      - "MINOR — selection tests unit collision circles, not sprite bounds (FR-030), so art changes cannot change gameplay."
      - "MINOR — valid placement defined: full 64px footprint passable, in bounds, unoccupied (FR-031)."
    open_items:
      - "5 open questions knowingly carried forward: unit balance numbers (tuning pass), map layout and ore-node placement (plan), final sprite ids (implementation), project name vs art direction (user, cosmetic), audio scope (plan). None block the bridge."
      - "Expect a fourth unspecified-ordering instance during planning — it is this codebase characteristic defect class."
  - phase: bridge
    decision: approved
    at: "2026-08-21T10:43:28Z"
    reviewed_sha: "93ec5861bf45d7e317936582ef72f8dca58d67f1"
    risk: low
    notes: "spec.md 451 lines: 31 FR (all traced), 15 US, 10 NFR + an 11-row NFR Measurement Contract, 23 TC + 9 E2E, 13-row codebase constraints table. 3 delta specs (simulation 8 / gameplay 6 / interface 17 FRs) covering 31/31. SpecKit mode classic. 11/11 quality checks pass. Sync-verify Layer 2 clean: FR and US sets match product-spec exactly, non-goals carried, all four key thresholds agree."
    decisions:
      - "Conditional sections omitted on evidence, not by default: EDA Events (no EDA patterns), Prerequisites (no related features), Consumer Contract (end-user not shared infrastructure)."
      - "contracts/openapi.yaml and asyncapi.yaml deliberately NOT generated. Empty stubs would be worse than absent: api-docs and sync-verify Layer 8 would validate real code against a fiction. contracts/README.md records the reasoning and names the command queue as the one internal contract, and as where asyncapi.yaml would begin if a backend ever arrives."
      - "Delta specs split into three domains mirroring the constitution sim/presentation split. Canonical specs/ does not exist yet, so all requirements are ADDED and these deltas create the domains."
    open_items:
      - "Plan MUST resolve two constitutional obligations concretely: the canonical state-hash definition (field order, entity ordering, exact float bits) and the replay-corpus format. Both must exist before the first bug fix or Constitution IV is unmeetable in practice."
      - "Plan MUST schedule the balance-tuning pass and the K1 comprehension playtest as real work. K1 is the only check on the fun goal and will otherwise be crowded out by the six machine-checkable criteria (RF-4)."
      - "Plan Complexity Tracking table starts EMPTY — zero accepted violations so far. Every later entry is a visible deliberate decision rather than accumulated drift."
      - "Expect a fourth unspecified-ordering determinism defect during planning; three found so far (FR-021, FR-022, FR-027)."
  - phase: plan
    decision: approved
    at: "2026-08-21T10:59:39Z"
    reviewed_sha: "c2ca5de4a474ca4cb08e85ea582f0e5695709ae9"
    risk: low
    notes: "plan.md 440 lines + 2 ADRs. Constitution Check 5/5 PASS at BOTH gates (before research, after design). Cross-validation 6/6. Coverage 12/12 stories, 31/31 FRs mapped to an owning milestone. Complexity Tracking EMPTY - zero accepted constitutional violations across five phases."
    obligations_discharged:
      - "OBLIGATION 1 - canonical state hash defined in ADR-001: fixed field order, id-ordered traversal, exact IEEE-754 bits via DataView. Rounding before hashing explicitly REJECTED (it would hide the sub-ulp divergence Principle I exists to detect). FNV-1a two 32-bit lanes to 64-bit hex, vendored, no dependency."
      - "OBLIGATION 2 - replay corpus defined in ADR-002: one JSON file per case, MANDATORY checkpoint hashes so failures localise rather than merely register, simVersion gating, and a deliberately manual PR-visible regeneration procedure. Auto-updating hashes on mismatch REJECTED outright as converting the Principle IV guard into a rubber stamp."
      - "OBLIGATION 3 - M8 (balance tuning) and M9 (K1 comprehension playtest) are numbered milestones with exit criteria, equal in standing to M0-M7. M9 BLOCKS completion. constants.ts exists as a single file so M8 has one tuning surface."
      - "OBLIGATION 4 - ordering audit found FOUR more hazards, not one. O-4 command application order within a tick (issuer, seq). O-5 production completion when ore covers only one factory (ascending factory id). O-6 simultaneous lethal damage - collect across tick, apply atomically at end, which also makes FR-028 Draw reachable rather than theoretical. O-7 entity iteration order - the ROOT of the class, fixed structurally by an id-sorted array plus lint bans on for-in and Map/Set iteration in src/sim."
    key_decisions:
      - "Build order inverted: M0 is the ESLint boundary config and 3-platform CI matrix BEFORE any game code. Its acceptance test is that lint FAILS on a deliberately planted Math.random() in src/sim - a guard nobody verified is a guard nobody has."
      - "Single package, not a monorepo. Principle II is about the dependency arrow, not package boundaries; a lint rule enforces it as strictly at a fraction of the ceremony."
      - "Simulation at 20 Hz. Phaser delta never crosses into step() - it only counts whole ticks and produces an interpolation alpha. Isolated in src/game/loop.ts so it is easy to review."
      - "One runtime dependency (Phaser). PRNG, A*, and hash vendored at ~50 lines total."
    open_items:
      - "Expect further ordering hazards as gameplay grows - seven catalogued, assume the list is incomplete. Any new pick-the-nearest/first/best needs a tie-break rule before the code is written."
      - "Cross-platform float divergence may surface in M1. That is wanted, not feared: fix the divergence, never loosen the hash."
      - "mulberry32 vs sfc32 decided in M1; plan recommends mulberry32 for its single-integer serialisable state."
  - phase: tasks
    decision: approved
    at: "2026-08-21T11:12:04Z"
    reviewed_sha: "7da597005ec4a5cb69cb1e73ca37795a8a558bdc"
    risk: low
    notes: "77 tasks across 10 milestones (M0-M9). Coverage 31/31 requirements, 12/12 Must+Should stories, 7/7 ordering hazards each with a dedicated test. Sizes 4 XS / 28 S / 37 M / 8 L / 0 XL. 24 tasks marked Test-first: true. Structural checks all pass: unique contiguous ids, every task carries Paths and Size, no orphans, no workspace prefixes (single-root)."
    findings:
      - "FIXED - adversarial ordering pass caught an inverted Red-Green cycle in M0: the ESLint boundary CONFIG preceded the test verifying it fires, in the very milestone whose point is that guards must be verified. Swapped - T003 is now the test (fails because no rules exist yet, so ESLint reports nothing) and T004 is the config that turns it green."
      - "DEVIATION (deliberate) - grouped by milestone rather than by user story. Story-first grouping would be fiction here: US-001 requires the whole game to exist, and Constitution I/II force the enforcement config and determinism harness before all gameplay. Every task carries its story/requirement tag so traceability is unaffected; only the grouping axis differs."
      - "traceability.yml requirements block seeded - 31 rows with stories and owning tasks. code and tests columns left to implement and test-plan."
    open_items:
      - "T040 (combat) is the L-sized task to watch - it carries TWO ordering hazards (O-1 acquisition, O-6 atomic damage) and is where a split would be least surprising if it grows."
      - "T075 is the first real exercise of ADR-002 manual corpus regeneration - the moment that discipline is either established or quietly abandoned."
      - "~51 of 77 tasks complete before a single pixel renders. Accepted in plan.md, but it will feel wrong around task 30."
      - "Watch for an eighth ordering hazard. The plan predicted more would appear and has already been right once."
  - phase: pre_impl_review
    decision: approved_with_conditions
    at: "2026-08-21T11:22:29Z"
    reviewed_sha: "7da597005ec4a5cb69cb1e73ca37795a8a558bdc"
    risk: low
    notes: "Focused review on untested assumptions rather than re-treading architecture (already 5/5 at two Constitution Check gates). Ten assumptions interrogated: seven held, three did not. 3 HIGH / 3 MEDIUM / 1 LOW. Found three HIGH findings four prior passes missed - because every earlier pass verified artifacts were consistent WITH EACH OTHER, while F-1/F-2/F-3 are each a MISSING STATE rather than a contradiction."
    conditions:
      - "F-1 RESOLVED via CR-001 (accepted) - sudden-death backstop. Ore exhaustion halts production but does not force resolution; a stalemate had no terminator. Escalating Base damage after global node depletion bounds match length; adds no fourth verdict and reuses the FR-028 Draw rule."
      - "F-2 ABSORBED - units do NOT collide in v1. No separation system, so A* stays a pure grid search and determinism is preserved; render-only jitter (T082) never touches sim state. A separation system would almost certainly be ordering hazard O-8. Legibility of the resulting pile becomes an explicit M9 question."
      - "F-3 ABSORBED - M9 exit criterion extended: >=3 of 5 first-timers must WIN at least one match on New to this. M8 tuned duration and M9 tested comprehension, but NOTHING tested beatability - the most common way a solo project ships an unwinnable game."
      - "F-4 ABSORBED into T048 - clamp accumulator to ~250ms and drop excess, so a player returning from a backgrounded tab does not watch the sim fast-forward through the match they just lost."
      - "F-5 ABSORBED into T073 - tune toward a legibility ceiling of ~25-30 units per side. The ~60 figure arrived as a performance NFR and was never a design decision; 60 units on 220 cells is 27% map occupancy with no camera to zoom out of."
      - "F-6 ABSORBED into T039/T080 - a Base with no surviving Workers affords one at zero cost, removing a dead state where a player can neither act nor lose."
      - "F-7 ABSORBED as T081 - 30-minute greyscale spike before T051. The underglow ring is the entire WCAG 1.4.1 mitigation and the drawn sprite is materially smaller than its 64px canvas."
  - phase: code_review
    decision: approved_with_conditions
    at: "2026-08-22T09:40:00Z"
    conditions_cleared_at: "2026-08-22T10:20:00Z"
    conditions_status: "ALL CLEARED — corrective pass. 197 tests green (was 179). SIM_VERSION 5 -> 6, corpus regenerated deliberately."
    risk: medium
    reviewed_by_model: null
    cross_model_findings: 0
    notes: "Machine gates all PASS (lint, types, 0/192 SCA, 95.05% stmts / 100% funcs). Ten findings: 2 CRITICAL, 3 HIGH, 4 MEDIUM, 1 LOW. Self-review — the same model wrote every line, so all four CRITICAL/HIGH findings were driven by executable probes rather than reading for intent."
    findings:
      critical: 2
      high: 3
      medium: 4
      low: 1
      fixed: 10
      acknowledged: 0
    root_cause: "No test drives the system through the command layer. Every M3 test hand-writes its preconditions into state — runProduction has 16 tests and all of them write queuedKind into a fixture, so nothing ever asked whether the only real-world producer of that field sets it. It does not. The production system is exhaustively verified and completely unreachable; 95% statements and 100% functions with the entry point to three of them broken."
    conditions:
      - "REV-001 CRITICAL — build commands never set queuedKind. Probe: 5000 ore, 400 ticks, 0 units produced, 0 ore spent, Base left in BUILDING state permanently. FR-011/FR-012/US-005/US-006 non-functional. MUST fix before M4."
      - "REV-002 CRITICAL — a build command aimed at a Factory makes it re-run its own construction, because queuedKind -1 plus state BUILDING matches the self-construction branch. This is the progress-with-two-meanings hazard M3's own digest flagged, biting in the same milestone it was written down in. MUST fix before M4."
      - "REV-003 HIGH — move commands unhandled; the comment justifying it says M2 will add the destination field, and M2 did (Amendment 2). Stale comment reads as justification. Probe: destX still -1 after 400 ticks. MUST fix before M6 input work."
      - "REV-004 HIGH — a destination inside a blocked cell skips the pathfinder entirely and the unit beelines through obstacles. Fires on EVERY worker deposit trip, since Base cells are blocked. Probe: worker crossed a walled column at a blocked row."
      - "REV-008 LOW but coupled — unchecked `queuedKind as Kind` cast NaNs the simulation on an out-of-range kind. Unreachable ONLY because REV-001 means queuedKind is never set from a command, so fixing REV-001 makes it reachable. Fix together."
      - "TEST GAP (HIGH) — add command-layer end-to-end tests before M4. M4 makes the AI a second command source, so the seam that hid these defects is about to get twice the traffic."
      - "CORPUS — 001-baseline.json contains two build commands that are silent no-ops, so its recorded hashes encode broken behaviour. Fixing REV-001 changes them legitimately; deliberate simVersion bump and regeneration required."
    positive:
      - "The 'would this test fail if the guard were deleted?' discipline has now paid three times (M0 empty config, M1 import Red, M2 tie-break deleted with all 10 tests still green). Very few codebases mutation-test their own guards by habit."
      - "Determinism architecture is sound: per-field hash mutation sweep, corpus reporting FIRST divergence rather than terminal mismatch, and a regen script that refuses to touch current cases — each the harder correct choice over an easier one that would have looked identical when green."
      - "ADR-001's four amendments record HOW each gap was found, not just what changed, including Amendment 4 admitting the regression suite caught a defect two milestones later."
    spec_updates_suggested: "none — every finding is code failing to meet the spec, not the spec failing to describe the code"
sync_runs:
  - at: "2026-08-21T11:22:29Z"
    scope: "quick — full chain after CR-001 application"
    verdict: "PASS (12/12, zero drift)"
    findings:
      - "spec.md 33 FRs; delta specs cover all 33; tasks coverage matrix covers all 33; traceability has 33 requirement rows."
      - "82 task ids unique, every task carries Paths and Size."
      - "journeys.yml and traceability.yml edge counts match exactly (JRN-001:10, JRN-002:2, JRN-003:2)."
      - "CR-001 marker present in spec.md, plan.md, tasks.md, and product-spec.md."
  - at: "2026-08-21T10:24:54Z"
    scope: "quick — Layer 1 (research <-> product-spec)"
    verdict: "PASS (1 drift found and resolved)"
    findings:
      - "All 8 research red flags (RF-1..RF-8) carried into product-spec."
      - "RESOLVED: US-012 (WebGL fallback) was covered by JRN-001 EDGE-001 but absent from that journey's stories list. Linked across journeys.yml, JRN-001 markdown, and traceability.yml."
      - "12/12 Must+Should user stories covered by a journey. US-013..015 are Could-Have/post-v1 and correctly uncovered."
      - "traceability.yml journey ids and edge counts match journeys.yml exactly."
change_requests:
  - id: "CR-001"
    title: "Sudden-death backstop for post-exhaustion stalemate"
    status: "accepted"
    timestamp: "2026-08-21T11:22:29Z"
    requested_at_phase: "pre_impl_review"
    artifacts_affected: 9
    tasks_added: 2
    phase_rollback: null
    rationale: "Pre-impl review F-1. Ore exhaustion halts production but does not force resolution; a post-exhaustion stalemate had no terminator, so the ~10-minute promise was not guaranteed by anything in the simulation. Four earlier review passes missed it because each checked artifacts for consistency WITH EACH OTHER, and FR-016/FR-017 are consistent - the gap is BETWEEN them, a state the requirements jointly fail to cover."

dependencies:
  depends_on: []
  depended_on_by: []

role_approvals:
  solo_mode: true
  required_roles_per_phase: {}
